{
  "name": "yt-zen",
  "private": true,
  "version": "3.16.6",
  "description": "Userscript-first YouTube customization with a resilient SponsorBlock integration",
  "license": "Unlicense",
  "scripts": {
    "build": "node scripts/build-userscript.js",
    "check": "node scripts/check.js && node scripts/check-release.js",
    "test:unit": "node scripts/test-unit.js",
    "test": "npm run build && npm run check && npm run test:unit && npm run test:zen && npm run test:boot",
    "test:boot": "node scripts/harness-boot.js",
    "test:zen": "node scripts/test-zen.js"
  },
  "engines": {
    "node": "^20.19.0 || ^22.13.0 || >=24.0.0"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
