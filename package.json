{
  "name": "yt-zen",
  "private": true,
  "version": "3.9.1",
  "description": "Userscript-first YouTube customization with a resilient SponsorBlock integration",
  "license": "Unlicense",
  "scripts": {
    "build": "node scripts/build-userscript.js",
    "check": "node --check yt-zen.user.js && for f in src/*.js; do node --check \"$f\"; done && node scripts/check-release.js",
    "test:unit": "node scripts/test-unit.js",
    "test": "npm run build && npm run check && npm run test:unit"
  },
  "engines": {
    "node": ">=18"
  }
}
